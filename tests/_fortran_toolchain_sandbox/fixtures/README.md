# Fixtures

This directory is reserved for small synthetic fixtures used by sandbox contract tests.

Do not place original EDDA case copies, executables, toolchains, installer payloads, or large grids here. Use `work/` for disposable case copies and `generated/` for generated artifacts.
